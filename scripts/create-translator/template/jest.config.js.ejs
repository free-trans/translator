const base = require('../../jest.config.base');

const packageName = '@arvinxu/translator-<%- engine %>';

const root = '<rootDir>/packages/api-<%- engine %>';

module.exports = {
...base,
rootDir: '../..',
roots: [root],
name: packageName,
displayName: packageName,
};

